
decideTweetMode:
  handler: src/decideTweetMode${self:custom.handlerFilePostfix}.handler
  description: Probabilistic tweet mode decision maker.
  environment:
    TEXT_PROBABILITY: 0.75
  memorySize: 128
  timeout: 10
